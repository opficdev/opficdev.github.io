# 디자인 검증

## 비교 대상

- 참고 화면: `output/playwright/research-banksalad-tech.png`
- 구현 홈 화면: `output/playwright/banksalad-inspired-home-final.png`
- 구현 다크 화면: `output/playwright/banksalad-inspired-home-dark.png`
- 구현 글 화면: `output/playwright/banksalad-inspired-post-final.png`
- 구현 모바일 화면: `output/playwright/banksalad-inspired-mobile.png`
- 통합 비교 화면: `output/playwright/banksalad-comparison.png`

## 비교 조건

- 참고 화면 픽셀 크기: 1440 x 1024
- 구현 홈 화면 픽셀 크기: 1440 x 1024
- 비교용 CSS 화면 크기: 1440 x 1024
- 브라우저 배율: 100%
- 장치 배율: 1
- 상태: 라이트 모드, 홈 최상단, 전체 분류 선택
- 밀도 보정: 같은 픽셀 크기와 장치 배율로 캡처해 별도 보정 없음

## 전체 화면 비교

`output/playwright/banksalad-comparison.png`에서 참고 화면과 구현 화면을 각각 1440 x 1024 원본 크기로 나란히 확인했습니다.

- 참고 화면의 얇은 상단 헤더, 넓은 본문 여백, 작은 `Stories` 제목, 태그와 제목과 요약 순서의 글 항목을 반영
- 사용자가 사진 자산을 제외하도록 요청했으므로 참고 화면의 대표 사진과 글 썸네일은 의도적으로 제외
- 사진 경로가 없는 상태에서는 빈 자리나 대체 상자를 만들지 않고 글 내용이 전체 폭을 사용하도록 처리
- 소개 문구와 사이드바를 제거하고 홈에서 바로 `Stories`와 글 목록이 시작되도록 처리

## 세부 화면 비교

참고 화면과 구현 화면을 같은 크기의 원본 캡처로 확인해 제목, 태그, 요약과 헤더 아이콘을 읽을 수 있었으므로 별도 확대 영역은 만들지 않았습니다. 글 상세 화면과 모바일 화면은 각각 독립된 원본 캡처로 본문 글꼴, 줄 길이와 반응형 배치를 확인했습니다.

## 필수 검증 항목

- 글꼴과 글자 체계: 한국어 시스템 산세리프 사용, 기본 16px 고정, 목록 제목 23px 수준, 본문 15px 수준으로 확인
- 간격과 배치: 1440px 화면에서 980px 글 목록을 가운데 배치하고 항목 사이를 얇은 구분선과 여백으로 분리
- 색상: 흰색과 짙은 회색을 기본으로 사용하고 선택 상태와 링크에만 초록색 사용
- 이미지 품질: 프로젝트 이미지 자산 없음, `header.teaser`가 있는 글만 실제 이미지를 렌더링하는 조건부 구조 확인
- 문구: 요청에 따라 소개 문구와 사이드바 하단 문구 제거
- 아이콘: Font Awesome 검색과 화면 모드 아이콘 표시 확인
- 반응형: 390 x 844 화면에서 헤더, 분류 필터와 글 목록이 겹치거나 가로로 넘치지 않는 상태 확인
- 접근성: 검색 입력 이름, 버튼 이름, 선택 상태, 본문 바로가기와 키보드 초점 요소 확인

## 동작 검증

- `iOS` 분류 선택 시 iOS 글 두 개만 표시
- 홈 검색에 `모듈` 입력 시 모듈 글 한 개만 표시
- 글 화면에서 `모듈` 검색 제출 시 `/?q=모듈`로 이동하고 같은 글 한 개만 표시
- 다크 모드와 라이트 모드 전환 및 페이지 이동 뒤 상태 유지 확인
- 마크다운 제목, 문단, 목록과 코드 블록의 HTML 변환 확인
- 브라우저 콘솔 오류와 경고 없음

## 비교 이력

1. 첫 화면에서 세부 태그 필터가 글보다 먼저 강조되는 문제 확인
   - 분류 필터를 `전체`, `iOS`, `Swift`, `아키텍처`, `개발 도구`로 축소
   - 세부 태그는 각 글 안에서만 표시하도록 수정
2. Minimal Mistakes의 큰 화면 기본 글자 비율로 간격과 글자가 커지는 문제 확인
   - `html` 기본 글자 크기를 16px로 고정
3. 테마의 `#main` 최대 너비로 홈 글 목록이 필요 이상으로 좁아지는 문제 확인
   - 홈 화면에만 전체 폭 규칙을 적용하고 글 목록을 980px로 가운데 정렬
4. 홈 전체 폭 규칙이 글 상세 화면에도 적용돼 본문이 왼쪽에 붙는 문제 확인
   - 글 상세 화면에 760px 너비와 가운데 정렬을 명시
   - 수정 뒤 `output/playwright/banksalad-inspired-post-final.png`에서 정렬 확인

## 남은 다듬기

- 실제 글 수와 태그 길이가 늘어난 뒤 분류 명칭과 한 줄 요약 길이를 다시 조정할 수 있음

## 최종 결과

final result: passed
